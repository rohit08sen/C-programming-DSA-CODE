/* Define following 3 functions as per given specification and call these 3 functions in driver function:
(i) inputSparse(a[][3]) to input a sparse matrix directly in triple form.
(ii) addSparse(a[][3],b[][3],c[][3]) to add to sparse matrices.
(iii) displaySparse(a[][3]) to display the resultant sparse matrix in triple form.*/